# Traxle Texture Pipeline

Use optimized texture sizes and only include maps that materially improve the scene.

Guidelines:
- Prefer compressed or modern web formats when possible.
- Avoid unnecessary 4K textures for web delivery.
- Use smaller mobile-specific textures for complex scenes.
- Remove unused roughness, normal, alpha, and emissive maps before shipping.
